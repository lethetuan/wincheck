# Chuẩn hóa xuống dòng: lưu LF trong repo cho mọi tệp văn bản.
* text=auto eol=lf

# Tệp nhị phân (không đụng tới xuống dòng).
*.png binary
*.ico binary
*.exe binary
*.ttf binary
*.woff binary
*.woff2 binary
